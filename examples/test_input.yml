grid_setting:
  N: 100
  L: 20.90
  N_p: 250

  input_file: input_files_new/input_coord250.csv
  restart_file: restart_files/restart_N100_step9999.csv

md_variables:
  N_steps: 20
  rescale: TRUE # rescaling of the initial momenta to have tot momenta = 0
  tol: 1.e-7
  init_steps: 0

output_settings: 
  restart: TRUE
  # path: output_files
  # dt # CHECK LATER
  # Temperature # NOT NOW
  # equilibriation -- later